{
  "name": "@everkano/personas",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "validate": "uv run --frozen --project ../../apps/api python scripts/validate_personas.py",
    "seed:generate": "uv run --frozen --project ../../apps/api python scripts/generate_seed.py",
    "lint": "uv run --frozen --project ../../apps/api python scripts/validate_personas.py"
  }
}
