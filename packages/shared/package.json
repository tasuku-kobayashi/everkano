{
  "name": "@everkano/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Web / API 共通の型定義（DB型は supabase gen types で生成、API型は FastAPI の OpenAPI と対応）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./database": "./src/database.types.ts",
    "./api": "./src/api.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
